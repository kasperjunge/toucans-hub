{
    "function_name": "evaluate_key_takeaways",
    "description": "Evaluate two sets of key takeaways based on given topics and determine which set best addresses the topics.",
    "properties": {
        "best_match": {
            "type": "string",
            "enum": ["KEY TAKEAWAYS A", "KEY TAKEAWAYS B", "EQUAL"]
        }
    },
    "required": [
        "best_match"
    ]
}

